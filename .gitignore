node_modules
dist
dist-ssr
.env
.env.local
.env.*.local
.vite
.vscode
.idea
.DS_Store
*.log
.netlify

# Internal docs (cotizaciones, planes de sprint, UAT) — no se versionan
docs/

# Legacy reference code (Vue / IMFD) — local only, no versionar
legacy/
transformation_loading/

# TypeScript incremental build info
*.tsbuildinfo

# ETL output — regenerated from data/source/ via npm run etl
public/data/investments.json
public/data/research.json
